<h1>Links</h1>

<ul>
  <% @links.each do |link| %>
    <li><%= link_to link.name, link_path(link)%> | votes: <%= link.votes %></li>
  <% end %>
</ul>

<hr>

<button class="btn"><%= link_to "New Link", new_link_path %></button>
